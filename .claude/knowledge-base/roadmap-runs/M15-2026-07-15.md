---
milestone_id: M15
slug: cost-token-visibility
date: 2026-07-15
status: in_progress
plan: .claude/knowledge-base/plans/cost-token-visibility-plan.md
review: null
release: null
checkbox_flipped_at: null
flip_commit_sha: null
---

# Milestone M15 — Cost & token visibility (TokenCostBreakdown + PriceBreakdown)

## Objective (from ROADMAP.md)
`TokenCostBreakdown` (input/output/cache tokens + custo, controlado) + `PriceBreakdown` (tabela por-unidade/1K/1M). O lens já tem inputTokens/outputTokens/costUsd → adoção real.

## Trail
- 2026-07-15: DISCOVER — contrato confirmado: lens tem `inputTokens`/`outputTokens` (compare-core.ts:19-20, trace-detail), `costUsd`/`totalTokens` (traces columns), `promptTokens`/`completionTokens` (experiments). Refs: phoenix `SpanTokenCosts.tsx` (breakdown por span) + langfuse `PriceBreakdownTooltip.tsx` (tabela de preço). Componentes puros/controlados, adotáveis em span/trace detail. Zero dep nova.
