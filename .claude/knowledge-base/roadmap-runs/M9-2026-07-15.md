---
milestone_id: M9
slug: sessions-rich
date: 2026-07-15
status: in_progress
plan: .claude/knowledge-base/plans/sessions-rich-plan.md
implementation: .claude/knowledge-base/implementations/sessions-rich-implementation.md
review: .claude/knowledge-base/reviews/sessions-rich-review-{date}.md
release: .claude/knowledge-base/releases/v{version}-release.md
checkbox_flipped_at: null
flip_commit_sha: null
---

# Milestone M9 — Sessions ricas (replay + métricas por sessão)

## Objective (from ROADMAP.md)

Fechar o maior stub do lens — hoje `sessions.tsx` é só uma lista (sessionId + traceCount). Promover `SessionTimeline` + `SessionSummary` ao DS e construir a tela de session detail (traces agrupados por sessão, métricas agregadas, navegação temporal).

## Definition of done (from ROADMAP.md)

- [ ] `SessionTimeline` + `SessionSummary` publicados no `@usetheo/ui` (stories + axe + testes + registry).
- [ ] theo-lens: tela de session detail (traces em ordem temporal, métricas: duração total, custo, tokens, erro, nº traces).
- [ ] North-star delta; zero dep nova de chart.
- [ ] Release semver + adoção no lens (deleção do stub).

## Trail

- 2026-07-15: milestone lockado. **Risco R1 RESOLVIDO no discover:** confirmado que NÃO existe rota `/sessions/{id}` no BFF (`lens_dashboard.go` só tem `.Get("/sessions", ...)`); a traces-list JÁ aceita `session_id` e retorna por trace `{traceId,name,status,model,startTimeUnixNano,durationMs,costUsd,totalTokens,sessionId}`. → **caminho UI-only** sobre os traces filtrados por sessão + agregação client-side. Zero backend novo (a mitigação registrada no ROADMAP).

## Outcome

(pending)
