---
milestone_id: M11
slug: analytics-timeseries
date: 2026-07-15
status: completed
plan: .claude/knowledge-base/plans/analytics-timeseries-plan.md
review: .claude/knowledge-base/reviews/analytics-timeseries-review-{date}.md
release: .claude/knowledge-base/releases/v{version}-release.md
checkbox_flipped_at: 2026-07-15
flip_commit_sha: (pending-commit)
---

# Milestone M11 — Analytics time-series SOTA (histogramas + percentis)

## Objective (from ROADMAP.md)

Elevar os dashboards do lens ao nível Langfuse/Phoenix. Promover `Histogram` + `PercentileChart` ao DS (SVG puro, ADR anti-chart-lib mantido).

## Definition of done (from ROADMAP.md)

- [ ] `Histogram` + `PercentileChart` publicados no `@usetheo/ui` (SVG puro, stories + axe + testes + registry).
- [ ] theo-lens: dashboards com latência percentil (banda), distribuição de duração (histograma) e top-models.
- [ ] North-star delta; zero dep nova de chart.
- [ ] Release semver + adoção no lens.

## Trail

- 2026-07-15: milestone lockado. **DISCOVER — contrato de backend CONFIRMADO (adotável ponta a ponta):** `/v1/dashboard/lens/observability` retorna `LatencyBucket {p50Ms,p95Ms,p99Ms}` por bucket (já consumido por `latencySeries()`); a traces-list retorna `durationMs`/`costUsd` por trace (binável client-side para histograma). **Escopo honesto (parsimony):** o lens JÁ sobrepõe p50/p95/p99 em 3 LINHAS no TrendChart (M3) — o `PercentileChart` é o UPGRADE para BANDAS sombreadas (padrão phoenix `TraceLatencyPercentilesTimeSeries`); o `Histogram` é o gap NET-NEW (langfuse `NumericScoreHistogram`, o lens não tem distribuição). Ambos fecham gaps SOTA reais, nenhum é YAGNI. Zero dep nova de chart (SVG puro, ADR do M3 mantido).

- 2026-07-15: PLAN analytics-timeseries v1.0 → plan-confidence **SHIPPABLE_WITH_CAVEATS 89** (caveat `symbol_fab_unverifiable_typescript` = refs a arquivos de código não-verificáveis pelo scorer M3 v0.2, deferido; não é fabricação confirmada). Gate ≥ SHIPPABLE_WITH_CAVEATS liberado.
- 2026-07-15: IMPLEMENT (TDD): **T1.0 computeHistogram** puro (8 testes, camada pura) · **T2.0 Histogram** composite (11 testes + axe, reusa linScale/niceMax) · **T3.0 PercentileChart** (bandas p50/p95/p99, reusa linScale/niceMax/seriesPath) · **T4.0 registry** (82 itens válidos). Full suite 1101/1101, typecheck/lint/format 0, build ESM ok.
- 2026-07-15: REVIEW (agente consolidado, lib-only) → **READY_TO_MERGE**. M-1 (MEDIUM: `bandPath` emitia NaN no path p/ percentil ausente — contradizia o "gap honesto") corrigido na raiz (quebra em sub-bandas contíguas, paridade com seriesPath do M3) + 2 regressões. Report: `.claude/knowledge-base/reviews/analytics-timeseries-review-2026-07-15.md`.
- 2026-07-15: RELEASE — descoberto que M9 (0.24.0) nunca foi merged→main (PR #12 estava aberto mas o `gh pr list` sem `--repo` falhava com o remote SSH custom `github-usetheo:`). Decisão do owner: **PR combinado 0.25.0** (M9+M11 empilhados; reescrever histórico é proibido). release chore 0.25.0 (`91b05795`), CHANGELOG com [0.24.0]+[0.25.0], push develop. **PR #12 atualizado** (título/body combinados) → `PR_OPEN_AWAITING_APPROVAL` (gate humano, Rule 4). 20 commits, MERGEABLE.

## Outcome

(pending)

## Outcome (final)

M11 CONCLUÍDO — release 0.25.0 (tag v0.25.0), adoção no lens (dashboards: banda de percentil migrada + histograma de duração), suíte 1696/1696, checkbox [x].
