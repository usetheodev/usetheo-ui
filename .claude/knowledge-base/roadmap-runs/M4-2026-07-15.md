---
milestone_id: M4
slug: stepper-promotion
date: 2026-07-15
status: in_progress
plan: .claude/knowledge-base/plans/stepper-promotion-plan.md
implementation: .claude/knowledge-base/implementations/stepper-promotion-implementation.md
review: .claude/knowledge-base/reviews/stepper-promotion-review-{date}.md
release: .claude/knowledge-base/releases/v{version}-release.md
checkbox_flipped_at: null
flip_commit_sha: null
---

# Milestone M4 — Stepper / PipelineStatus (promoção do dashboard)

## Objective (from ROADMAP.md)

Promover o padrão `build-timeline` do dashboard para um composite genérico de progresso multi-etapas — o pipeline de ingest do theo-rag (partitioned → chunked → embedded → indexed → ready) e futuros fluxos de onboarding.

## Definition of done (from ROADMAP.md)

- [ ] `Stepper` composite com estados por etapa (pending/active/done/failed), orientações horizontal/vertical, timestamps opcionais e suporte a etapa com erro + retry slot; DoD padrão da lib.
- [ ] API validada contra os dois casos reais: ingest pipeline do theo-rag e build timeline do dashboard.
- [ ] Story de composição com `StatusDot`/`Badge` existentes (sem duplicar semântica de status).

## Outcome

(pending)

## Trail

- DISCOVER (pré-staged durante o gate do PR #4, artefatos `.claude/` apenas — ADR D4 do discovery plan): plan v1.1 SHIPPABLE_WITH_CAVEATS 89 → blueprint SHIPPABLE_WITH_CAVEATS 89 (`.claude/knowledge-base/discoveries/blueprints/stepper-promotion-blueprint.md`).
