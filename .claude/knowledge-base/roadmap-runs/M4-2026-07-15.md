---
milestone_id: M4
slug: stepper-promotion
date: 2026-07-15
status: completed
plan: .claude/knowledge-base/plans/stepper-promotion-plan.md
implementation: .claude/knowledge-base/implementations/stepper-promotion-implementation.md
review: .claude/knowledge-base/reviews/stepper-promotion-review-2026-07-15.md
release: .claude/knowledge-base/releases/v0.20.0-release.md
checkbox_flipped_at: 2026-07-15 (pós-merge PR #5)
flip_commit_sha: pending-commit
merge_commit: f08464ed75971182550f9d07bacb0fb22e839de8  # PR #5
flip_audit: .claude/knowledge-base/roadmap-runs/M4-2026-07-15-flip-audit.md
---

# Milestone M4 — Stepper / PipelineStatus (promoção do dashboard)

## Objective (from ROADMAP.md)

Promover o padrão `build-timeline` do dashboard para um composite genérico de progresso multi-etapas — o pipeline de ingest do theo-rag (partitioned → chunked → embedded → indexed → ready) e futuros fluxos de onboarding.

## Definition of done (from ROADMAP.md)

- [x] `Stepper` composite com estados por etapa (pending/active/done/failed), orientações horizontal/vertical (data-orientation), timestamps e retry como slots; DoD padrão (29 testes, axe zero violations nas 2 orientações, biome/typecheck/build verdes).
- [x] API validada contra os dois casos reais: stories BuildPipelineFailed (dashboard, com retry) e IngestPipeline (theo-rag, com timestamps).
- [x] Story ComposicaoStatus com `StatusDot`/`Badge` — imports só na story; componente reusa apenas tokens.

## Outcome

Ciclo completo (discover 89 → plan v1.1 89 → implement 5/5 + validation exit 0 → code-quality PASS_WITH_CAVEATS 89 → review READY_TO_MERGE com 4 MEDIUMs fixados in-cycle sob RED-GREEN — incl. bug real do guard `in`/prototype-chain — → release). PR #5 mergeado (merge `f08464ed`); tag `v0.20.0`; GitHub release; `@usetheo/ui@0.20.0` no npm com `latest: 0.20.0` verificado. Suite 829/829.

## Trail

- DISCOVER (pré-staged durante o gate do PR #4, artefatos `.claude/` apenas — ADR D4 do discovery plan): plan v1.1 SHIPPABLE_WITH_CAVEATS 89 → blueprint SHIPPABLE_WITH_CAVEATS 89 (`.claude/knowledge-base/discoveries/blueprints/stepper-promotion-blueprint.md`).
