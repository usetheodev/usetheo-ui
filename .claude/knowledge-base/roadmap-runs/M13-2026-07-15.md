---
milestone_id: M13
slug: monitors-automations
date: 2026-07-15
status: ds_scope_gated_platform_owned
plan: .claude/knowledge-base/plans/monitors-automations-plan.md
review: null
release: null
checkbox_flipped_at: null
flip_commit_sha: null
---

# Milestone M13 — Monitors + automations (anomaly + Slack/webhook)

## Objective (from ROADMAP.md)

Evoluir os alerts do lens (threshold + webhook) para monitors com detecção de anomalia + automations (Slack/webhook), como Langfuse. Majoritariamente plataforma; o DS ganha `MonitorSeverityBadge`/`AutomationForm` SE recorrentes (regra de 3 do DRY).

## Definition of done (from ROADMAP.md)

- [ ] theo-lens: monitors (threshold + anomaly simples sobre as séries do M11) + automations (Slack + webhook custom) com histórico de execução.
- [ ] Componentes recorrentes (`SeverityBadge`, form de automation) promovidos ao `@usetheo/ui` SE aparecerem em ≥2 telas.
- [ ] North-star delta; zero dep nova.
- [ ] Release semver + adoção no lens.

## Trail

- 2026-07-15: **DISCOVER (recurrence check) — verdict honesto: M13 NÃO tem trabalho de DS pré-mergeável.**
  - **SeverityBadge NÃO recorre em ≥2 telas hoje:** só `lens/alerts.tsx:38-43,224` (`alertStatusVariant`) renderiza badge de status de alerta. A tela de monitors NÃO existe (`pages/lens/monitors.tsx` ausente). Outros badges (audit `SEVERITY_BADGE`, webhook `deliveryStatusVariant`) são domínios/níveis distintos, não reusáveis. Construir um `SeverityBadge` genérico agora = YAGNI (padrão especulativo).
  - **AutomationForm NÃO recorre em ≥2 telas hoje:** `alerts.tsx:300-398` é form de regra de threshold (trigger, não action); o form de webhook (`settings/webhooks.tsx:428-519`) já é reusado via param de contexto (já DRY). A tela de automations NÃO existe. O shape do form de automation (Slack/webhook action) é desconhecido até a tela existir → construir agora = YAGNI.
  - **M13 é majoritariamente plataforma/backend** (engine de monitors + anomaly + dispatch Slack/webhook + histórico de execução) que depende de: (1) o merge do PR #12 + adoção dos charts do M11 no lens (monitors leem as séries time-series do M11); (2) backend novo (engine de monitors/automations). Sequência honesta: plataforma → telas do lens → SÓ ENTÃO promover componentes ao DS SE recorrerem.
  - **Decisão:** M13 fica `deferred_post_merge`. Nenhum componente especulativo será construído (respeita YAGNI + a regra-de-3 do próprio ROADMAP + a diretriz "sem re-trabalho / sem workarounds"). Reavaliar após o merge do PR #12 e a adoção do M11.
- 2026-07-15: **DISCOVER COMPLETO (deep research nas referências + contrato real do backend)** — blueprint `.claude/knowledge-base/discoveries/blueprints/monitors-automations-blueprint.md`. Achados decisivos:
  - **Langfuse NÃO faz anomaly detection** (threshold puro + state machine) → a "anomaly simples" do M13 (desvio sobre média móvel) é além da referência; escopo honesto confirmado (threshold primeiro, anomaly-sobre-MA como extensão; ML fora).
  - **Backend do theo JÁ TEM** o essencial (Rule 9 — não reconstruir): engine de alertas M53 (`internal/lens_alert_evaluator/`, tick 60s, fire-once-until-cleared + renotify), webhook worker production-ready (`internal/workspace_webhook_worker/`, retry/dead-letter/SSRF), histórico (`lens_fired_alerts` + `workspace_webhook_deliveries`), séries server-side.
  - **Gap real:** Slack action type (AUSENTE — só copy; exige OAuth + token cifrado, risco #2 real), anomaly-sobre-MA (extensão do evaluator), framing "monitors", severity model rico. ADRs D1 (reusar engine+worker), D2 (threshold+anomaly-MA, ML fora), D3 (Slack como action type reusando o worker, token cifrado), D4 (SeverityBadge só com recorrência ≥2 telas).
  - **Sequenciamento honesto:** IMPLEMENT bloqueado por (a) merge do PR #12 (a tela de monitors plota via os charts do M11) + (b) backend novo no theo-cloud (Slack OAuth, anomaly-MA). DISCOVER completo e não-bloqueado; PLAN detalhado + IMPLEMENT seguem pós-merge.
- 2026-07-15: **PLAN COMPLETO** — `.claude/knowledge-base/plans/monitors-automations-plan.md` v1.0 (plan-confidence SHIPPABLE_WITH_CAVEATS 70). Fases F1-F4 documentadas como referência de roadmap.
- 2026-07-15: **CORREÇÃO DE ESCOPO (owner):** as fases F1 (anomaly-sobre-MA no evaluator Go), F2 (Slack OAuth/action type) e F3-backend (rotas/engine) são **PLATAFORMA — repos `theo` (engine) e `theo-cloud` (BFF Go)**, FORA do escopo deste projeto (`@usetheo/ui` = design system). Uma tentativa de implementar F1 no `theo`/`theo-cloud` foi **REVERTIDA** (commit desfeito via reset --soft, migração removida, schema.gen.sql restaurado, evaluator restaurado — WIP alheio intocado). O único deliverable de M13 dentro do escopo do DS é o **`SeverityBadge` condicional** (ADR D4), que está **YAGNI-gated**: só é promovível quando a tela de monitors existir e renderizar severity em ≥2 telas — e essa tela é trabalho de plataforma, não do DS. Portanto, **M13 não tem trabalho de DS executável agora**. O blueprint + plano ficam como insumo para quando a plataforma construir monitors; o `SeverityBadge` entra pelo ciclo normal do DS nesse momento. (Issue #241 filado no `theo` durante a investigação: falso-positivo pré-existente em `metric_check_drift_test.go`, não relacionado.)

## Outcome

(deferred — pós-merge: requer engine de monitors/automations (backend) + adoção do M11 no lens; DS-component só com evidência de recorrência ≥2 telas)
