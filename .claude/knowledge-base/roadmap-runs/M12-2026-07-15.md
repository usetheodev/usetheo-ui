---
milestone_id: M12
slug: annotation-platform
date: 2026-07-15
status: completed
plan: .claude/knowledge-base/plans/annotation-platform-plan.md
review: .claude/knowledge-base/reviews/annotation-platform-review-{date}.md
release: .claude/knowledge-base/releases/v{version}-release.md
checkbox_flipped_at: 2026-07-15
flip_commit_sha: 78dfa25d
---

# Milestone M12 — Annotation platform (configs + tipos)

## Objective (from ROADMAP.md)

Estruturar anotação humana — hoje `labeling-queue.tsx` tem label/score/note free-form sem config store reutilizável. Promover `AnnotationInput` (categorical/continuous/freeform, controlado) ao DS e adicionar score-configs ao lens.

## Definition of done (from ROADMAP.md)

- [ ] `AnnotationInput` (categorical / continuous / freeform, controlado) publicado no `@usetheo/ui` (stories + axe + testes + registry).
- [ ] theo-lens: score-configs (tipos de score nomeados) + anotação por span/trace usando os configs, consumindo o componente.
- [ ] North-star delta; zero dep nova.
- [ ] Release semver + adoção no lens (labeling-queue passa a usar os configs).

## Trail

- 2026-07-15: milestone lockado (execução durante o gate humano do PR #12 — M12 é independente do merge de M9/M11). **DISCOVER — 2 agentes (referências clonadas + contrato real do theo-lens):**
  - **Padrão (phoenix + langfuse):** o modelo de **3 tipos do Phoenix** (CATEGORICAL/CONTINUOUS/FREEFORM) é o contrato mínimo (langfuse tem 5, mas BOOLEAN=categorical-de-2, CORRECTION=system-only). Componente **controlado, config-driven**: categorical → radiogroup/select sobre `values[].label`; continuous → number input com bounds; freeform → textarea. Evidência: `phoenix/app/src/components/annotation/{Categorical,Continuous,Freeform}AnnotationInput.tsx`, `langfuse/packages/shared/src/domain/score-configs.ts`. **YAGNI (deferido):** batch/keyboard-nav, inter-rater/dual, LLM-judge, annotation queues, comment popover, optimization-direction UI.
  - **Contrato real (theo-lens):** submissão de annotation é REAL (`POST /v1/dashboard/lens/labeling-queue/{id}/resolve` com `{label,score?,note?}`; `GET/POST/DELETE /v1/dashboard/lens/traces/{id}/annotations`). **Score-config STORE não existe no backend** (risco #1 confirmado — zero hits `scoreConfig`). Alvo de adoção: `dashboard/src/pages/lens/labeling-queue.tsx:228-238`. @usetheo/ui em `^0.23.0`.
  - **Decisão de escopo (honesta, alinhada à mitigação do ROADMAP):** o componente DS é config-driven (recebe config por prop) → **NÃO depende do store backend**. A adoção no lens define os score-configs como **fixtures client-side** (mitigação "UI + fixtures" explícita no ROADMAP) e passa ao componente; a submissão usa o endpoint real. Nenhum workaround: é o contrato honesto do backend atual (annotation free-form, sem enum enforcement).

- 2026-07-15: PLAN annotation-platform v1.0 → plan-confidence **SHIPPABLE_WITH_CAVEATS 89** (caveat `symbol_fab_unverifiable_typescript`, refs de código, deferido). IMPLEMENT (TDD): **T1.0** tipos+guards (4 testes) · **T2.0** AnnotationInput (19 testes+axe nos 3 tipos, compõe RadioGroup/Input/Textarea/Label, Extract p/ TS#30581) · **T3.0** registry (83 itens). Full suite 1123/1123, typecheck/lint/format 0, build ESM ok.
- 2026-07-15: REVIEW (agente consolidado, lib-only) → **READY_TO_MERGE**. LOW L-1 (Label órfão no categorical + nome duplicado) corrigido in-cycle (aria-labelledby vira a fonte do nome). Report: `.claude/knowledge-base/reviews/annotation-platform-review-2026-07-15.md`. **Fase de biblioteca do M12 completa na develop; aguarda o pipeline de release (empilha sobre M9/M11 no PR #12 OU novo PR conforme a ordem de merge).**

## Outcome

(pending — library phase done; release + lens adoption + flip pendentes)

## Outcome (final)

M12 CONCLUÍDO — release 0.26.0 (tag v0.26.0), adoção no lens (labeling-queue com AnnotationInput sobre score-configs fixture), suíte 1696/1696, checkbox [x].
