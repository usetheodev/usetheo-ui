---
milestone_id: M3
slug: trendchart-promotion
date: 2026-07-15
status: in_progress  # MILESTONE_IN_FLIGHT — PR #4 (0.19.0) aguardando aprovação humana
plan: .claude/knowledge-base/plans/trendchart-promotion-plan.md
implementation: .claude/knowledge-base/implementations/trendchart-promotion-implementation.md
review: .claude/knowledge-base/reviews/trendchart-promotion-review-{date}.md
release: .claude/knowledge-base/releases/v{version}-release.md
checkbox_flipped_at: null
flip_commit_sha: null
---

# Milestone M3 — TrendChart (promoção do dashboard)

## Objective (from ROADMAP.md)

Promover o `metric-trend-chart.tsx` do dashboard (SVG puro, dependency-free, a11y via tabela) para composite `TrendChart` genérico da lib — a base dos analytics do theo-rag (volume, latência p50/p95) e das métricas de uso do theo-memory.

## Definition of done (from ROADMAP.md)

- [ ] `TrendChart` composite multi-série com legend, tooltip nativo, formatters de eixo e fallback `<table>` acessível; zero dependência de chart lib (mantém ADR).
- [ ] Código seeded do dashboard com API generalizada (sem nomes de domínio lens/deploy); DoD padrão da lib.
- [ ] Story reproduzindo o caso analytics do theo-rag (série temporal p50/p95) e o caso de uso do dashboard atual.

## Outcome

(pending)
