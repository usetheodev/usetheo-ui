# Release v0.20.0 — Stepper (M4)

**Date:** 2026-07-15 · **Verdict:** PR_OPEN_AWAITING_APPROVAL

- **Input verdict:** review READY_TO_MERGE (`.claude/knowledge-base/reviews/stepper-promotion-review-2026-07-15.md`)
- **Bump:** minor (0.19.0 → 0.20.0 — `[Unreleased] § Added` não-vazio, sem BREAKING/Removed)
- **Release commit:** `chore(release): 0.20.0` (CHANGELOG promovido + package.json + registry:build por último)
- **PR:** https://github.com/usetheodev/usetheo-ui/pull/5 (develop → main, human gate — Unbreakable Rule 4)
- **Notes:** `.claude/knowledge-base/releases/v0.20.0-notes.md`

## Pós-merge checklist

1. Tag anotada `v0.20.0` no merge commit + `gh release create` + `pnpm publish --access public --publish-branch develop` + verificar `npm dist-tag ls` (latest=0.20.0).
2. Flip M4 `[ ]`→`[x]` via `flip_milestone_checkbox.py` + mv audit para `.claude/knowledge-base/roadmap-runs/` + run-file `completed`.
3. → M5 (FileDropzone — referência react-dropzone; ADR dep-vs-own).
