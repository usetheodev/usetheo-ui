# @usetheo/ui

Violet Forge design system — the generic React components + primitives that form the non-AI foundation of the Theo UI stack. Apache-2.0, ESM-only.

**54 components** (39 primitives + 15 composites) + the Violet Forge foundation (`cn`, tailwind-preset, themes). Depended on by `@theokit/ui` (the AI-native layer).

> Seeded from `theo-ui` @ `2b46eca` (2026-07-03, AI-exclusive pivot milestone M-B).
