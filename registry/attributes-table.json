{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "attributes-table",
  "type": "registry:ui",
  "title": "AttributesTable",
  "description": "Namespace-grouped OTel span attributes with fail-closed PII masking. A masked value's raw string never enters the DOM until Reveal is clicked AND canReveal is true; object values fall back to the dependency-free JsonViewer. Promoted-key badges + collapsible per-namespace cards.",
  "dependencies": ["lucide-react"],
  "registryDependencies": [
    "badge",
    "button",
    "card",
    "copy-button",
    "empty-state",
    "json-viewer",
    "trace",
    "tailwind-preset"
  ],
  "files": [
    {
      "path": "components/composites/attributes-table/attributes-table.tsx",
      "type": "registry:ui",
      "target": "components/ui/attributes-table.tsx"
    }
  ]
}
